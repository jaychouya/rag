# feat: 重构为检索-精判架构，消除递归，提升速度且不降准确性

检索阶段采取「多召不漏」策略：混合关键词全文/向量语义搜索，放宽候选数量，并保留法条完整层级路径作为上下文，确保正确法条几乎不被遗漏。

精判阶段改为一次性全局对比：将所有候选送入同一轮 LLM，利用条款间对比推理提升匹配精度，避免逐层递归的上下文割裂与错误传导。

整体将 LLM 调用次数从树规模相关降为常数，大幅降低延迟，同时通过高召回与全局比较保证法律适用准确性。
